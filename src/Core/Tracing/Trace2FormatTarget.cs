namespace GitCredentialManager;

/// <summary>
/// The different format targets supported in the Trace2 tracing system.
/// </summary>
public enum Trace2FormatTarget
{
    Event,
    Normal,
    Performance
}
